package dev.digitalducktape.openride

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Placeholder test proving the JUnit test setup works end to end. Real tests land in
 * later tickets (T2 sensor abstraction, T4 Room data layer, T5 ride engine).
 */
class ExampleUnitTest {
    @Test
    fun additionIsCorrect() {
        assertEquals(4, 2 + 2)
    }
}
